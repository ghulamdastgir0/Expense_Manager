// ============================================================
// src/context/SettingsContext.jsx
// ONLY context (no logic, no hooks)
// ============================================================

import { createContext } from "react"

const SettingsContext = createContext(null)

export default SettingsContext